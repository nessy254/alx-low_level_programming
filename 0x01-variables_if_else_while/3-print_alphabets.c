#include <stdio.h>

/**
 * main - start of the program
 * Return: if successful returns zeri
 */
int main(void)
{
	for (char n = 'a'; n <= 'z'; n++)
	{
		putchar(n);
	};
	for (char n = 'A'; n <= 'z'; n++)
	{
		putchar(n);
	}

	putchar('\n');
	return (0);
}


